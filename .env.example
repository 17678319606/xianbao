# 线报酷 -> WordPress 发布凭据（仅服务器本地使用，切勿提交到仓库！）
# 复制本文件为 .env 并填入真实值：cp .env.example .env
WP_SITE=https://12313.icu
WP_USER=tougao
WP_APP_PASSWORD=你的WordPress应用密码（形如 xxxx xxxx xxxx xxxx）

# 可选：循环模式（--loop / systemd）下的采集间隔，单位秒，默认 300（5 分钟）
# XIANBAO_INTERVAL=300

# ============ 失败告警（可选，建议配置） ============
# 1) 企业微信机器人 webhook：在企微群「设置 -> 群机器人 -> 添加」获取，
#    形如 https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxx
WXWORK_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=ca5e918f-8e98-4c96-9734-8e5d27b298d0

# 2) 邮件告警（SMTP）：留空则不发邮件，仅走企微机器人
ALERT_EMAIL=weixinkaifa@jinbufenzi.work
# 发信 SMTP 配置（按你的邮件服务商填写；以下为示例，需替换为真实值）
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_TLS=1
SMTP_USER=alert@your-domain.com
SMTP_PASS=你的SMTP授权码

# ============ 可选：GitHub 保活（仅当你重新开启 GitHub Actions 定时才需要） ============
# 作用：每周空提交一次并 push，防止 GitHub 因 60 天无仓库活动而禁用定时任务。
# 留空则跳过保活（推荐：运行时已迁移到服务器，本就无需依赖 GitHub 定时）。
# 填法：GitHub 个人访问令牌（需 repo 写权限）。
GH_TOKEN=
